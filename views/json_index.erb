[
<% @family.each_with_index do |person,index| %>{
 "id": <%= person.id %>, 
 "name": "<%= person.name %>", 
 "uri": "<%= @base_host %><%= person.id %>", 
 "dad_uri": "<%= @base_host %><%= person.dad_id %>", 
 "mom_uri": "<%= @base_host %><%= person.mom_id %>"
}<%="," if index != @family.length - 1 %>
<% end %>
]
